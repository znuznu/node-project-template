import { expect, describe, it } from 'vitest';

describe('Unit test template', () => {
    describe('when ...', () => {
        it('should ...', () => {
            expect(1).toEqual(1);
        });
    });
});
